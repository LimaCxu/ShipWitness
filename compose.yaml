services:
  shipwitness:
    build: .
    image: shipwitness:0.2.0
    ports:
      - "4173:4173"
    volumes:
      - shipwitness-data:/app/data
    restart: unless-stopped

volumes:
  shipwitness-data:
