{
  "name": "shipwitness",
  "version": "0.4.0-dev.2",
  "description": "Evidence-first release acceptance for AI-built software.",
  "license": "MIT",
  "private": false,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check lib/store.js && node --check lib/postgres-store.js && node --check lib/auth.js && node --check lib/browser-executor.js && node --check scripts/migrate-json-to-postgres.js && node --check scripts/backup.js && node --check scripts/verify-backup.js && node --check scripts/restore.js && node --check outputs/shipwitness-prototype/app.js && node --check outputs/shipwitness-prototype/api.js && node --test",
    "db:migrate-json": "node scripts/migrate-json-to-postgres.js",
    "backup": "node scripts/backup.js",
    "backup:verify": "node scripts/verify-backup.js",
    "restore": "node scripts/restore.js"
  },
  "dependencies": {
    "pg": "^8.23.0",
    "playwright": "^1.62.1"
  }
}
